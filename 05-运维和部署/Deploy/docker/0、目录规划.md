# Docker 部署目录规划

Docker 部署统一使用 `/data/docker/<服务名>/` 作为宿主机挂载根目录。

与 yum/二进制安装路径 `/data/<服务名>/<版本>/` 区分开，避免混用。

> 环境准备（安装 Docker、镜像加速、一键建目录）见 [1、环境准备.md](./1、环境准备.md)  
> **偏生产**（挂载示例配置、compose 检查清单）见 [2、偏生产部署说明.md](./2、偏生产部署说明.md)

## 统一目录结构

```
/data/docker/
├── mysql/
│   ├── conf/          → /etc/mysql/conf.d
│   ├── data/          → /var/lib/mysql
│   └── logs/          → /var/log/mysql
├── redis/
│   ├── conf/          → redis.conf
│   └── data/          → /data
├── nginx/
│   ├── conf/          → nginx.conf
│   ├── conf.d/        → /etc/nginx/conf.d
│   ├── html/          → /usr/share/nginx/html
│   └── logs/          → /var/log/nginx
├── clickhouse/
│   ├── conf/          → /etc/clickhouse-server
│   ├── data/          → /var/lib/clickhouse
│   └── logs/          → /var/log/clickhouse-server
├── zookeeper/
│   ├── data/          → /bitnami/zookeeper/data
│   └── logs/          → /bitnami/zookeeper/logs
├── kafka/
│   └── data/          → /bitnami/kafka/data
├── rabbitmq/
│   ├── data/          → /var/lib/rabbitmq
│   └── logs/          → /var/log/rabbitmq
├── fastdfs/
│   ├── tracker/       → /var/fdfs
│   └── storage/       → /var/fdfs
├── jenkins/
│   └── data/          → /var/jenkins_home
├── nacos/
│   ├── logs/          → /home/nacos/logs
│   └── data/          → /home/nacos/data（Derby 模式）
└── portainer/
    └── data/          → /data（Portainer 配置与数据）
```

**偏生产示例配置**（复制到上表对应宿主机目录）见 [conf/](./conf/)，说明见 [2、偏生产部署说明.md](./2、偏生产部署说明.md)。

```
docker/conf/                    # 仓库内示例，部署时 cp 到 /data/docker/.../
├── mysql/my.cnf
├── redis/redis.conf
├── nginx/nginx.conf
├── nginx/conf.d/default.conf
├── clickhouse/users.d/default-password.xml
└── rabbitmq/rabbitmq.conf
```

## 服务清单

| 服务 | Docker 支持 | 文档 | compose |
| ---- | ----------- | ---- | ------- |
| MySQL | ✅ | [3、mysql.md](../3、mysql.md) | [mysql.yml](./mysql.yml) |
| Redis | ✅ | [5、redis.md](../5、redis.md) | [redis.yml](./redis.yml) |
| Nginx | ✅ | [4、nginx.md](../4、nginx.md) | [nginx.yml](./nginx.yml) |
| ClickHouse | ✅ | [1、clickhouse.md](../1、clickhouse.md) | [clickhouse.yml](./clickhouse.yml) |
| Zookeeper | ✅ | [8、zookeeper.md](../8、zookeeper.md) | [zookeeper.yml](./zookeeper.yml) |
| Kafka | ✅ | [9、kafka.md](../9、kafka.md) | [kafka-kraft.yml](./kafka-kraft.yml)（KRaft）/ [kafka-zk.yml](./kafka-zk.yml)（ZK）；UI 见 [kafka-ui.yml](./kafka-ui.yml) |
| RabbitMQ | ✅ | [10、RabbitMq.md](../10、RabbitMq.md) | [rabbitmq.yml](./rabbitmq.yml)（挂 conf/） |
| FastDFS | ✅ | [11、fastdfs.md](../11、fastdfs.md) | [fastdfs.yml](./fastdfs.yml) |
| Jenkins | ✅ | [12、jenkins.md](../12、jenkins.md) | [jenkins.yml](./jenkins.yml) |
| Nacos | ✅ | [13、nacos.md](../13、nacos.md) | [nacos.yml](./nacos.yml) / [nacos-mysql.yml](./nacos-mysql.yml) |
| Portainer | ✅ | [14、portainer.md](../14、portainer.md) | [portainer.yml](./portainer.yml) |
| JDK | ❌ | [2、jdk.md](../2、jdk.md) | 基础镜像内置 |
| Oracle | ⚠️ | [6、oracle.md](../6、oracle.md) | 授权复杂，建议二进制 |
| FRP | ⚠️ | [7、frp.md](../7、frp.md) | 建议二进制 |

## 待补充（常用中间件）

以下中间件 Docker 部署也很常见，可按需补充文档：

| 服务 | 用途 | 典型镜像 | 优先级 |
| ---- | ---- | -------- | ------ |
| MinIO | 对象存储（S3 兼容） | `minio/minio` | ⭐⭐⭐ 可替代部分 FastDFS 场景 |
| Elasticsearch | 全文检索 / 日志存储 | `elasticsearch:8.x` | ⭐⭐⭐ 配合 Kibana 使用 |
| Prometheus + Grafana | 监控告警 | `prom/prometheus`、`grafana/grafana` | ⭐⭐⭐ 运维监控 |
| Harbor | Docker 私有镜像仓库 | `goharbor/harbor` | ⭐⭐ 内网镜像管理 |
| PostgreSQL | 关系型数据库 | `postgres:16` | ⭐⭐ 部分项目替代 MySQL |
| MongoDB | 文档型数据库 | `mongo:7` | ⭐⭐ NoSQL 场景 |
| GitLab | 代码托管 + CI/CD | `gitlab/gitlab-ce` | ⭐⭐ 可替代 Jenkins + 自建 Git |
