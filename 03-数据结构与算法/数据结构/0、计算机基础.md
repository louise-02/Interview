# 1、原码

原码就是符号位加上真值的绝对值，即用第一位表示符号，其余位表示值。

[+1]原 = 0000 0001

[-1]原 = 1000 0001

# 2、反码

反码的表示方法是：正数的反码是其本身。负数的反码是在其原码的基础上, 符号位不变，其余各个位取反。

[+1] = [00000001]原 = [00000001]反

[-1] = [10000001]原 = [11111110]反

# 3、补码

补码的表示方法是：正数的补码就是其本身。负数的补码是在其原码的基础上，符号位不变，其余各位取反，最后+1。(即在反码的基础上+1)。

在计算机系统中，数值一律用补码来表示(存储)

[+1] = [00000001]原 = [00000001]反 = [00000001]补

[-1] = [10000001]原 = [11111110]反 = [11111111]补

# 4、位移

- 正数

- 负数

  [-3] = [10000011]原 = [11111100]反 = [11111101]补 -> 右移一位 = [11111110]补 = [11111101]反 = [00000010]原 

# 5、进制转换

- 十进制转二进制整数

  除2取余，逆序排列

- 十进制转二进制小数

  乘2取整，顺序排列

# 6、 有符号取值

以有符号8位为例，最大值01111111为127，最小值10000000为-128，其中-128的原码并不存在，是一个特定的值。

取值可以理解为8为取值从0到255，负数从后面255开始取值，255：11111111为-1，254：11111110为-2

# 7、二进制减法

```
//例如 5（101）- 3（011）
首先先计算-3的补码，即取反加1，得到-3的补码为：101

  101 (5)
+ 101 (-3的补码)
-----
 1010

 两个用补码表示的数相加时，如果最高位（符号位）有进位，则进位被舍弃。
 最后，舍弃最高位的进位，得到结果为010（2），所以5（101）- 3（011）= 2（010）
```

