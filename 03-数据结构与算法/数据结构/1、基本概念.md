# 1、线性结构

数据元素之间存在一对一的关系。

线性结构有两种不同的存储结构，**顺序存储结构，链式存储结构**。

顺序存储的线性表成为顺序表，**顺序表中存储的元素是连续的（内存）**。

链式存储的线性表称为链表，**链表中的存储元素不一定是连续的**，元素节点中存放数据元素以及相邻元素地址信息。

线性结构常见的有 **数组、队列、链表、栈** 。

# 2、非线性结构

二维数组、多维数组、广义表、树结构、图结构